# EXECPLAN — Progress Log

Dated log of completed work for the backend + MVC admin repo. Append one entry per finished task/milestone, newest at the bottom of each phase. See `DECISIONS.md` for locked decisions and `superpowers/specs/` for the authoritative design.

## Phase 0 — Inception & setup

| Date | Done | Commit | Next |
|---|---|---|---|
| 2026-06-30 | Read PRD + 3 MVP source reports; confirmed toolchain (.NET 9 SDK 9.0.305, Flutter 3.44 / Dart 3.12) | — | — |
| 2026-06-30 | Brainstormed Phase-1 design; resolved forks (DEC-1..DEC-9) | — | — |
| 2026-06-30 | Wrote Phase-1 backend-spine design spec | `33bc28e` | review |
| 2026-06-30 | Restructured to `backend/` + `mobile/` workspace; preserved history; init mobile repo | (this commit) | — |
| 2026-06-30 | Locked DEC-10/11/12 (workspace, EN+AR admin, Material theme); updated spec | (this commit) | — |
| 2026-06-30 | Added CLAUDE.md, DECISIONS.md, PROGRESS.md; working-agreement to keep these current | (this commit) | writing-plans → scaffold |

## Phase 1 — Backend spine (in progress)

Executing `docs/superpowers/plans/2026-06-30-execplan-backend-spine.md` on branch `feat/backend-spine` (subagent-driven, TDD, review gate per task).

| Date | Task | Done | Commit | Tests |
|---|---|---|---|---|
| 2026-06-30 | T1 | Solution scaffold: 5 src + 2 test projects, clean-architecture refs, pinned 9.0.*, secret-safe config, smoke test | `0d4e0aa` | 1/1; build 0 warn/0 err |
| 2026-06-30 | T2-T4 | Domain layer: BaseEntity + 9 locked enums (T2); template-side entities Organization/Department/User/Plan(+Contacts/Activators)/Team/TeamMembership/ShiftAssignment/TaskTemplate (T3); runtime-snapshot entities PlanActivation/ActivationParticipant/ExecutionTask/NotificationLog/CallAttempt/ResponseStatus/EscalationLog/BroadcastMessage (T4) | `d0bdd04`, `f39d394`, `7a8ce78` | 5/5 unit tests passing (smoke + BaseEntityTests x2 + TemplateGraphTests + RuntimeGraphTests); build 0 warn/0 err |
| 2026-06-30 | T5-T6 | Infra/persistence layer: 6 Application abstractions (IClock/IRepository/IUnitOfWork/INotificationProvider/IRealtimeNotifier/ICurrentUser); ExecPlanDbContext + 19 EF configs (18 domain entities + infra-only RefreshToken) with app-assigned Guid keys (ValueGenerated.Never) and CreatedAtUtc/UpdatedAtUtc stamping on SaveChangesAsync; Repository\<T\>/UnitOfWork; AddInfrastructure (SqlServer/Sqlite provider switch, registers UoW/repo only this wave); KuwaitClock (T6) wired as IClock; DesignTimeDbContextFactory + generated InitialCreate migration (not applied to any DB) | `f31382b`, `7231cf6` | 7/7 tests passing (5 unit + 2 new SQLite-in-memory integration: plan round-trip + CreatedAtUtc stamping); build 0 warn/0 err |
| 2026-06-30 | T7 | Auth services: `IPasswordHasher`/`IJwtTokenFactory`/`IRefreshTokenStore`/`IAuthService` + `TokenPair`/`AppUserPrincipal` records in Application/Auth (Application stays EF/Infra-free — `IRefreshTokenStore` abstracts the Infra-only `RefreshToken` entity per locked Task-7 dependency-boundary correction); minimal `AppException` (Kind enum + 5 factories, `ErrorKind` property) added early in Application/Common since AuthService needs it ahead of Task 10; Infra `IdentityPasswordHasher` (wraps `PasswordHasher<object>`), `JwtTokenFactory` (HS256, claims sub/role/name/jti, reads `Jwt:*` config), `RefreshTokenStore` (stages add/revoke, no SaveChanges — `AuthService` owns the single transaction); `AuthService.LoginAsync`/`RefreshAsync` (rotate+revoke-old)/`ValidateCredentialsAsync`, all timestamps via `IClock`; registered in `AddInfrastructure` (refresh-days from `Jwt:RefreshTokenDays`, default 14) | (this commit) | 12/12 tests passing (5 unit + 7 integration: 2 prior + 5 new `AuthServiceTests` on `TestClock`/SQLite fixture — login success/failure, refresh rotation+old-token-revoked, unknown-token, expired-token); build 0 warn/0 err |
| 2026-06-30 | T7 follow-up (auth review fixes, DEC-14) | Closed the Important finding + 2 Minor hardenings from the auth review: (1) `IRepository<T>` gains `FirstOrDefaultAsync(predicate)`/`ListAsync(predicate?)` (BCL `Expression<Func<T,bool>>`, no EF in Application); `Repository<T>` implements both via EF async no-tracking queries; converted the 3 sync-over-async call sites (`AuthService.FindActiveUserByUserName`→`FindActiveUserByUserNameAsync`, `RefreshTokenStore.FindActiveAsync`, `RefreshTokenStore.RevokeAsync`—tracked via `Tracking().FirstOrDefaultAsync`, still stages only, no `SaveChanges`); (2) closed the login username-enumeration timing side-channel — unknown-username path now runs a real `IPasswordHasher.Verify` against a lazily-computed fixed dummy PBKDF2 hash before throwing, same as the wrong-password path; (3) null/empty-guarded `userName`/`password` in the new shared `AuthenticateAsync` helper (used by both `LoginAsync`/`ValidateCredentialsAsync`) so missing creds throw the same `AppException.Unauthorized` instead of an unhandled exception | `b2f7727` | 14/14 tests passing (5 unit + 9 integration: 7 prior + 2 new `Repository<Plan>.FirstOrDefaultAsync`/`ListAsync` checks in `PersistenceTests`); the 5 existing `AuthServiceTests` still pass unchanged; build 0 warn/0 err |
| 2026-06-30 | T8 | Host auth wiring: `Program.cs` calls `AddInfrastructure`, registers MVC controllers (+views) and `ICurrentUser`→`CurrentUser` (Api-layer, reads `IHttpContextAccessor`), and configures auth — JWT bearer as the default authenticate/challenge scheme (HS256, issuer/audience/lifetime validated from `Jwt:*`, `MapInboundClaims=false` + `RoleClaimType=ClaimTypes.Role`/`NameClaimType="name"` matched exactly to what `JwtTokenFactory` emits) plus a reserved `"AdminCookie"` scheme (`LoginPath=/admin/login`) for the future MVC admin area; 5 role policies (`Admin`/`Manager`/`Leader`/`Member`/`ManagerOrAdmin`) in `Api/Auth/AuthPolicies.cs` over the `UserRole` enum-name claim values. Anonymous `AuthController` (`POST login`/`refresh` → `IAuthService`, `AppException.Unauthorized`→401) and `[Authorize]` `WhoAmIController` (`GET /api/v1/whoami`) prove the pipeline end to end. `TestAppFactory` (`WebApplicationFactory<Program>`) hosts the real app over one shared SQLite `:memory:` connection, seeds an admin user; **gotcha hit + fixed:** `Program.cs` reads `Database:Provider` and `Jwt:*` eagerly via `builder.Configuration[...]` at builder-build time, before `WebApplicationFactory`'s `ConfigureWebHost` overrides are spliced in (those only apply at the `Build()` call boundary inside `Program.cs`, by which point `AddInfrastructure`/the JWT options already read the original `appsettings.json` values) — first symptom was EF's "only a single database provider can be registered" (stray `UseSqlServer` wiring), second was 401-with-valid-token (bearer validated against the original signing key while `JwtTokenFactory` signs with the override, since it reads config lazily per-request); fixed by also setting the same overrides as process environment variables (`Database__Provider`, `Jwt__*`) in the factory's constructor, which **are** visible to `WebApplication.CreateBuilder`'s default config sources from the start | `c3a8e97` | 17/17 tests passing (5 unit + 12 integration: 9 prior + 3 new `AuthEndpointTests` — 401 with no token, login+bearer whoami 200/SystemAdmin, wrong-password 401); build 0 warn/0 err |
| 2026-06-30 | T8 follow-up (review hardening) | Closed 3 small review findings: (1) `TestAppFactory` now unsets (`= null`) the same `Jwt__*`/`Database__Provider` process env vars it sets, in `Dispose(bool)`, from a single `EnvVarKeys` array derived from `ConfigOverrides.Keys` (no separate hard-coded list to drift) — prevents cross-fixture env pollution if another `WebApplicationFactory` is added later; (2) `ExecPlanDbContext` stamping refactored into a private `StampTimestamps()` called from both funnel overrides `SaveChanges(bool)` and `SaveChangesAsync(bool, CancellationToken)` (removed the narrower `SaveChangesAsync(CancellationToken)` override) so sync saves now stamp `CreatedAtUtc`/`UpdatedAtUtc` too, with no double-stamping; (3) `Program.cs`'s JWT options now read `Jwt:Issuer`/`Jwt:Audience` through the same `?? throw new InvalidOperationException(...)` guard already used for `Jwt:SigningKey`, so a misconfigured issuer/audience fails fast at startup instead of silently rejecting all tokens | (this commit) | 18/18 tests passing (5 unit + 13 integration: 17 prior + 1 new `PersistenceTests.SaveChanges_sync_stamps_CreatedAtUtc_on_insert` proving the sync path stamps); build 0 warn/0 err |
| 2026-06-30 | T11-T12 | **ActivationService** (Application/Activation, `IActivationService.ActivateAsync(planId, actingUserId)`) — the core one-tap activation: guards (plan exists → NotFound; authorized = SystemAdmin OR plan creator OR `PlanActivator` row, else Forbidden; already-Active activation → Conflict) → `KuwaitShiftCalculator.Resolve(clock.UtcNow)` for band+rosterDate → on-duty = `ShiftAssignment` rows for the plan's teams matching band+date with `SubstituteForUserId==null` (empty → Conflict "no one on duty"), substitute rows build a `substituteFor[substitutedUser]=standIn` map → freezes a `PlanActivation{Active, EscalationThreshold=EscalationOptions.DefaultThreshold(5)}` + one `ActivationParticipant` per on-duty user (TeamNameSnapshot frozen, `ResolvedSubstituteUserId` from the map) + one `ExecutionTask` per team `TaskTemplate` (`DueAtUtc=ActivatedAtUtc+tpl.Duration`, snapshot Title/Order/SourceTaskTemplateId) → stages notification + call attempt #1 per participant via `INotificationProvider` and sets `CallAttemptCount=1` → **single `SaveChangesAsync` (EF implicit tx, atomic NFR-8; no `BeginTransactionAsync`)** then `IRealtimeNotifier.DashboardChangedAsync`. New `EscalationOptions{DefaultThreshold=5}` in Application/Common (shared with later EscalationService). **AcknowledgeService** (Application/Execution, +`IAcknowledgeService`) — the single counted readiness tap: find participant by `(ActivationId,UserId)` (else Forbidden); if no existing `ResponseStatus` add one (`AcknowledgedAtUtc=clock`) and flip participant→`Ready` (re-loaded tracked via `GetByIdAsync` so the no-tracking predicate read still persists), single SaveChanges + realtime push; idempotent (second tap finds the row, no-ops). Children added via `Repo<T>().AddAsync` with ctor-assigned Guid FKs (no tracked-parent nav). Not yet DI-wired (deferred to later AddApplication task; tests construct directly). | (this commit) | 44/44 tests passing (18 unit unchanged; 26 integration: 15 prior + 8 new `ActivationServiceTests` — full snapshot/participants/2-tasks-each/DueAt=ActivatedAt+Duration/2 notifications+2 call#1/`CallAttemptCount==1`, substitute freeze, already-active→Conflict, empty-roster→Conflict, unauthorized→Forbidden, `PlanActivator` non-creator allowed, admin allowed, missing-plan→NotFound; + 3 new `AcknowledgeTests` — sets Ready + one ResponseStatus, idempotent twice, non-participant→Forbidden); build 0 warn/0 err |
| 2026-07-01 | T13 | **EscalationService** (Application/Escalation, `IEscalationService.RunCycleAsync(activationId)`→`EscalationCycleResult(AttemptsAdded,Inducted)`) — one escalation cycle (design §5.4, FR-ESC-1..4): load activation via `GetByIdAsync` (null→NotFound; `Status!=Active`→Conflict "cannot escalate a closed activation") → load still-Pending participants **TRACKED** (mutation must persist) → per pending participant `++CallAttemptCount` + `StageCallAttempt` + `attemptsAdded++`; at `CallAttemptCount>=activation.EscalationThreshold` (the per-activation COPIED value, not EscalationOptions) flip `Status=Escalated` and, when `ResolvedSubstituteUserId!=null` AND no existing participant already has `InductedFromParticipantId==p.Id` (idempotency guard via `FirstOrDefaultAsync`), **induct the frozen substitute**: new `ActivationParticipant{IsSubstitute=true, InductedFromParticipantId=p.Id, ResolvedSubstituteUserId=null, Pending, CallAttemptCount=1}` + a FRESH task set regenerated from the team's `TaskTemplate`s (`DueAtUtc=now+Duration`, snapshot Title/Order/SourceTaskTemplateId) + `StageNotification` to the sub + `StageCallAttempt #1` + `EscalationLog{ParticipantId=p.Id, SubstituteUserId=sub.UserId, NewParticipantId=sub.Id}`, `inducted++`. **Single `SaveChangesAsync` (stage-all-then-save, EF implicit tx, atomic NFR-8; no `BeginTransactionAsync`)** then `DashboardChangedAsync`. **STEP 1 prerequisite:** `IRepository<T>` gains `FirstOrDefaultTrackedAsync`/`ListTrackedAsync` (BCL `Expression`, Application stays EF-free); `Repository<T>` implements both via the TRACKED `_db.Set<T>()` (no `AsNoTracking`). Identical dashboard/CLI path. Not yet DI-wired (deferred to AddApplication task; tests construct directly). | (this commit) | 49/49 tests passing (18 unit unchanged; 31 integration: 26 prior + 5 new `EscalationServiceTests` — at-threshold non-responder escalated + substitute inducted with full task set/1 notification/call#1/EscalationLog while Ready member untouched (`AttemptsAdded=1`/`Inducted=1`), below-threshold one cycle adds +1 to each pending and inducts nobody (`AttemptsAdded=2`/`Inducted=0`), Ready participant never escalated across 2 cycles, closed activation→Conflict, unknown activation→NotFound); build 0 warn/0 err |
| 2026-06-30 | T9-T10 | `KuwaitShiftCalculator`/`ShiftResolution` in Application/Shifts (pure, resolves Asia/Kuwait via `TimeZoneInfo.FindSystemTimeZoneById` with Arab Standard Time fallback; Morning 06-14/Evening 14-22/Night 22-06, night-after-midnight h<6 → previous day's roster); not yet DI-registered (deferred to a later AddApplication task). `AppException` already had all 5 `Kind` values + factories from the T7 follow-up — added the missing unit test coverage only, no production changes. `DatabasePlaceholderProvider : INotificationProvider` (Infrastructure/Notifications) stages `NotificationLog`/`CallAttempt` rows via `IUnitOfWork.Repo<T>().AddAsync` (no SaveChanges — caller owns the transaction) and `NoOpRealtimeNotifier : IRealtimeNotifier` (Infrastructure/Realtime, both methods `Task.CompletedTask`) as a stand-in until the real SignalR notifier lands; both registered in `AddInfrastructure` (the registration deferred from T5-T6) | (this commit) | 33/33 tests passing (18 unit: 5 prior + 7 new `KuwaitShiftCalculatorTests` + 6 new `AppExceptionTests`; 15 integration: 13 prior + 2 new `DatabasePlaceholderProviderTests` — stage-then-SaveChangesAsync round-trip for both `StageNotification` and `StageCallAttempt`, plus a pre-save assertion proving stage-only/no-autosave); build 0 warn/0 err |
| 2026-07-01 | T14 (+DEC-16) | **DashboardService** (Application/Dashboard, `IDashboardService.GetSnapshotAsync(activationId)`→`DashboardDto`) — the single server-side aggregated monitoring payload (design §5.5, FR-MON-1/2/3). Pure read: load activation via `GetByIdAsync` (null→NotFound), then one no-tracking `ListAsync` per set (participants, executionTasks, notificationLogs, callAttempts, responseStatuses, escalationLogs, broadcastMessages) and aggregate in memory — DEC-14, no multi-collection Includes/cartesian explosion; Application stays EF-free (LINQ-to-objects). **Counters** Total + Pending/Ready/Escalated/Inducted (mutually exclusive per DEC-16, Total=sum). **Rates** `ResponseRate=Ready/Total`, `TaskCompletionRate=Done/TasksTotal` (÷0→0). **Per-team rows** grouped by `(TeamId,TeamNameSnapshot)` with Members/ReadyCount/TasksTotal/TasksDone and `Score=0.5*readyRatio+0.5*doneRatio` (÷0→0), ordered Score DESC (best→delayed). **Overdue** tasks `Status==Pending && DueAtUtc<clock.UtcNow` projected to `OverdueTask(TaskId,Title,ParticipantUserId,DueAtUtc)` (user via task.ParticipantId→participant). **Feed** union of 6 sources (notification/call/response/escalation/task-done/broadcast)→`FeedEvent(AtUtc,Type,Text)`, `OrderByDescending(AtUtc).Take(50)`. New nested records `DashboardDto`/`TeamRow`/`OverdueTask`/`FeedEvent`. No save, no realtime push. Not yet DI-wired (deferred to AddApplication task; tests construct directly). **STEP 0 also applied DEC-16:** `EscalationService` inducted-substitute `Status` `Pending`→`Inducted` (the cycle still loads only `Status==Pending`, so substitutes are not re-escalated; `Inducted→Ready` on acknowledge); updated the matching `EscalationServiceTests` assertion. | (this commit) | 55/55 tests passing (18 unit unchanged; 37 integration: 31 prior + 6 new `DashboardServiceTests` — counts-by-status across activate/ack/escalate with Total==sum, response+completion rates, 2-team best→delayed ordering, overdue lists only past-due Pending, events capped at 50 newest-first; the 5 `EscalationServiceTests` still green after the DEC-16 status change); build 0 warn/0 err |

| 2026-07-01 | Review fixes (STEP 0) | Three consistency fixes from review: (1) `ExecPlanDbContext.StampTimestamps` now sets `CreatedAtUtc` on `Added` **only when still `default(DateTime)`** so service/clock-set values (e.g. provider-staged feed rows) survive instead of being overwritten by wall-clock — both the sync `SaveChanges(bool)` and async `SaveChangesAsync(bool,ct)` funnels still call it; `UpdatedAtUtc=DateTime.UtcNow` on `Modified` unchanged. (2) `EscalationService` sets `EscalationLog.CreatedAtUtc=_clock.UtcNow` explicitly. (3) `DashboardService` feed excludes `Broadcast`-kind `NotificationLog`s (they surface via the dedicated broadcast source — no double-listing), the `response` feed `Text` is now identity-free (`"ready"`, no raw user guid), and the misleading "no-tracking throughout" comment corrected to scope it to the `ListAsync` set-loads. | `e551f9a` | 55/55 unchanged (18 unit + 37 integration); build 0 warn/0 err |
| 2026-07-01 | T15 | **ExecutionService** (Application/Execution) + **BroadcastService** (Application/Broadcast), both reading the actor from injected **`ICurrentUser`** (methods take only operation params). `UpdateTaskAsync(taskId,done?,note?,reassignToParticipantId?)` — task TRACKED via `GetByIdAsync` (null→NotFound), source participant loaded; **done/note-only** allowed for owner OR source-team leader OR manager/admin (else Forbidden); **reassign** requires target in the SAME activation (else Validation), and a leader must lead BOTH source AND target teams (else Forbidden — cross-team 403) while manager/admin may reassign anywhere; applies `done`→`Status/CompletedAtUtc`, `note`, owner change; single SaveChanges + `DashboardChangedAsync`. `SetSubstituteLiveAsync` (mgr/admin or team leader → `ResolvedSubstituteUserId`), `RaiseIssueAsync` (TeamLeader-only → stages a `Notification`-kind `NotificationLog` to `plan.CreatedByUserId`), `CloseAsync` (mgr/admin; already-Closed→Conflict; `Status=Closed`+`ClosedAtUtc`; `ActivationClosedAsync`; returns final `DashboardDto`). BroadcastService `BroadcastAsync` (mgr/admin; one `BroadcastMessage`+one `Broadcast`-kind `NotificationLog` per participant via the provider seam; ONE SaveChanges; push). All stage-then-ONE-SaveChanges (atomic, NFR-8; no `BeginTransactionAsync`); mutated entities loaded TRACKED; realtime after commit. **Note:** ExecutionService also injects `INotificationProvider` (RaiseIssue staging) beyond the brief's listed deps. New reusable `FakeCurrentUser` test double. | `859bd95` | 65/65 (18 unit + 47 integration: 37 prior + 10 new `ExecutionAndBroadcastTests` — own-task done+note, other-member→Forbidden, leader in-team reassign, leader cross-team→Forbidden, cross-activation→Validation, set-substitute, raise-issue→creator notification, broadcast=1 msg+3 notifications, close→Closed+DashboardDto, re-close→Conflict); build 0 warn/0 err |
| 2026-07-01 | T16 | **`AddApplication(IServiceCollection, IConfiguration)`** (Application/DependencyInjection.cs) — singleton `EscalationOptions{DefaultThreshold=cfg.GetValue<int?>("Escalation:DefaultThreshold") ?? 5}` + singleton `KuwaitShiftCalculator`; scoped `IActivationService→ActivationService`, `IEscalationService→EscalationService`, `IDashboardService→DashboardService`, `AcknowledgeService`, `ExecutionService`, `BroadcastService`. Application stays EF/SignalR-free — added only the framework-neutral `Microsoft.Extensions.DependencyInjection.Abstractions` / `Configuration.Abstractions` / `Configuration.Binder` packages (pinned `9.0.*`). Wired `builder.Services.AddApplication(builder.Configuration)` into `Program.cs` immediately after `AddInfrastructure(...)` (`ICurrentUser→CurrentUser` already registered by the host). Every Application service now exists and is DI-wired. | `a98cef4` (+DI smoke test) | 67/67 (18 unit + 49 integration: 47 prior + 2 new `DependencyInjectionTests` resolving all Application services from the REAL host container + asserting the default threshold=5); build 0 warn/0 err |
| 2026-07-01 | T15-16 follow-up (review fixes) | Closed the Important + Minor findings from the execution/broadcast review: (1) **8 new negative-authorization tests** in `ExecutionAndBroadcastTests.cs` pinning every security gate — member attempting `UpdateTaskAsync(..., reassignToParticipantId:...)`→Forbidden (members cannot reassign at all, even within their own team), non-leading leader calling `SetSubstituteLiveAsync` for a team they don't lead→Forbidden, member calling `RaiseIssueAsync`→Forbidden, member AND leader calling `BroadcastAsync`→Forbidden, member AND leader calling `CloseAsync`→Forbidden; (2) **`BroadcastService.BroadcastAsync`** now guards before fan-out: `_cur.UserId is null`→`AppException.Forbidden` (was an unguarded `_cur.UserId!.Value` NRE risk on a malformed principal), and loads the activation via `GetByIdAsync`→`AppException.NotFound` when missing (previously a bogus `activationId` would still write an orphan `BroadcastMessage` since only `ActivationParticipant` rows were queried, which silently no-op on zero matches); covered by a new `Broadcast_to_unknown_activation_throws_NotFound` test. No change to the authorization logic itself, the manager-authority model, or RaiseIssue's role-only scoping — all confirmed correct by the prior review. | (this commit) | 75/75 (18 unit unchanged; 57 integration: 49 prior + 8 new `ExecutionAndBroadcastTests`); build 0 warn/0 err |
| 2026-07-01 | T18 | **Activation/execution/dashboard REST endpoints + global error middleware + PRD §21 acceptance test.** New `AppExceptionMiddleware` (Api/Middleware) maps `AppException.Kind` → HTTP (NotFound→404, Forbidden→403, Unauthorized→401, Conflict→409, Validation→400; any other unhandled→500) writing a small `{ error, kind }` JSON body; registered FIRST in the `Program.cs` pipeline (before `UseAuthentication`). 3 thin controllers: `PlansActivateController` (`POST plans/{id}/activate`, `ManagerOrAdmin`, acting user from `ICurrentUser`→401-if-null, returns `{ activationId }`); `ExecutionTasksController` (`PATCH execution-tasks/{id}`, `[Authorize]`, body `{done?,note?,reassignToParticipantId?}`→`ExecutionService.UpdateTaskAsync`); `ActivationsController` (`api/v1/activations`) — `GET {id}/dashboard` (`Roles=SystemAdmin,PlanManager,TeamLeader`→`IDashboardService`), `POST {id}/acknowledge` (`[Authorize]`→`AcknowledgeService`), `POST {id}/run-escalation` (`ManagerOrAdmin`→`IEscalationService`, returns `EscalationCycleResult`), `POST {id}/broadcast` (`ManagerOrAdmin`→`BroadcastService`), `POST {id}/set-substitute`+`POST {id}/raise-issue` (`[Authorize]`→`ExecutionService`), `GET {id}/my-tasks`+`my-notifications` (`[Authorize]`, filtered to the caller's participant / `RecipientUserId`, empty if not a participant), `POST {id}/close` (`ManagerOrAdmin`→`ExecutionService.CloseAsync`→`DashboardDto`). All endpoints return DTO records/projections, never raw entities; object-level authz stays in the services. **GOTCHA:** `AddApplication` registers `AcknowledgeService`/`ExecutionService`/`BroadcastService` as CONCRETE types (only `IActivationService`/`IEscalationService`/`IDashboardService` have interfaces), so controllers inject the concrete types. `TestAppFactory` now swaps the default `KuwaitClock` for a fixed `TestClock` (2026-06-30 08:00 UTC = 11:00 Asia/Kuwait → Morning/2026-06-30) via `RemoveAll<IClock>()`+`AddSingleton<IClock>(_clock)` and exposes `FixedUtcNow`/`FixedShift` so the acceptance test seeds a roster aligned to the host's deterministic shift (`Escalation:DefaultThreshold` left at default 5 — DI test still asserts 5; JWT expiry uses real `DateTime.UtcNow` so tokens stay valid). `AcceptanceFlowTests` drives the full §21 cycle over real HTTP: seed (org/manager/2 members/substitute/plan/team/2 templates/3 roster rows via DbContext scope + real hasher) → manager activates (200, capture id) → member1 acknowledges (200) → dashboard Ready==1/Pending==1 → bounded run-escalation loop until member2 Escalated (==1) + substitute Inducted (==1, verified in store) → member1 `my-tasks` returns exactly its 2 tasks (disjoint from member2's) → close (200, store Status=Closed) → follow-up dashboard 200 → re-close→409 (proves the middleware Conflict mapping). | `(this commit)` | 81 (18 unit + 63 integration: 62 prior + 1 new `AcceptanceFlowTests`); build 0 warn/0 err |
| 2026-07-01 | T17 | **CRUD REST controllers** (8 controllers, `src/ExecPlan.Api/Controllers/`) over `api/v1/{organizations,departments,users,plans,teams,team-members,shift-assignments,task-templates}`, role-gated per PRD §14: Organizations/Departments/Users — reads `ManagerOrAdmin`, writes `Admin`-only; Plans/Teams/TeamMembers/ShiftAssignments/TaskTemplates — reads+writes `ManagerOrAdmin` (plan authoring). Every endpoint returns an explicit DTO record, never the raw entity — `UsersController` never serializes `PasswordHash`. `UsersController.Create` hashes via `IPasswordHasher`; `Update` only ever touches FullName/Phone/Role/DepartmentId/IsActive (no username/password/org change). `PlansController.Create` sets `CreatedByUserId` from `ICurrentUser.UserId` and stages optional nested Contacts/Activators via `Repo<PlanContact>()/Repo<PlanActivator>().AddAsync` keyed off the ctor-assigned `Plan.Id` (never mutates the tracked parent's nav collection, per CLAUDE.md convention 2); `Get` reassembles them from a filtered `ListAsync` for the response. All writes call exactly one `SaveChangesAsync`; mutating reads (`GetByIdAsync`) rely on `Repository<T>.GetByIdAsync`'s already-tracked query (no `*TrackedAsync` needed for single-row update/delete). `TeamMembersController` maps the `team-members` route onto the existing `TeamMembership` entity (brief's name vs. domain's name). New `CrudAndVisibilityTests.cs` pins the authorization matrix end-to-end over `TestAppFactory`/real HTTP: unauthenticated GET users→401; admin create+read-back round trips for user (asserts response body excludes `PasswordHash`/`passwordHash`) and org/department; manager (created+logged-in via the new admin Users API) can GET users (200) and POST plans (200) but cannot POST users (403); member cannot GET users (403). | `5d34fd7` | 80/80 (18 unit unchanged; 62 integration: 57 prior + 5 new `CrudAndVisibilityTests`); build 0 warn/0 err |
| 2026-07-01 | T18 review follow-up (DEC-17) | Closed the 2 Important (inherited) findings + 1 Minor test gap from the T18 review. (1) **`DashboardDto` contract restored**: added `ActivationStatus Status`/`ShiftBand Shift`/`DateTime RosterDate` (positioned right after `ActivationId`); counter names (`TotalParticipants`/`PendingCount`/`ReadyCount`/`EscalatedCount`/`InductedCount`) kept exactly as-is per DEC-17. `DashboardService.GetSnapshotAsync` now populates the three fields straight from the already-loaded `PlanActivation`. (2) **Leader-own scoping on `GET activations/{id}/dashboard`**: after the existing `Roles=SystemAdmin,PlanManager,TeamLeader` gate, the action now additionally checks — for `TeamLeader` callers only — that they lead at least one team with a participant in this activation (`ActivationParticipant.TeamId`→`Team.TeamLeaderUserId==_cur.UserId`), else throws `AppException.Forbidden` (403 via the existing middleware mapping); Manager/Admin remain unscoped. `IDashboardService.GetSnapshotAsync` itself stays untouched/actor-agnostic. (3) **Test coverage**: new `DashboardServiceTests.Snapshot_reflects_activation_status_shift_and_roster_date` (Active→Closed via a real `ExecutionService.CloseAsync` + fresh snapshot); extended `ExecutionAndBroadcastTests.Close_sets_status_closed_and_returns_dashboard` with a `snapshot.Status` assertion; new `DashboardAccessTests.cs` (3 HTTP-level facts: leading-team leader→200, unrelated-team leader→403, manager→200 regardless); `AcceptanceFlowTests` close assertions now read `dto.Status==Closed` directly off the close/GET responses instead of a DbContext-scope workaround (re-close→409 check kept), and gained `My_notifications_returns_only_the_callers_own_rows` (Minor gap — member1's broadcast notification row is disjoint from member2's, reusing `SeedScenario`/`LoginAsAsync`). | `(this commit)` | 86/86 (18 unit unchanged; 68 integration: 81 prior total + 5 new — 1 `DashboardServiceTests`, 1 new assertion in an existing test (no count change), 3 `DashboardAccessTests`, 1 `AcceptanceFlowTests`); build 0 warn/0 err |

| 2026-07-01 | T19 | **In-process SignalR `DashboardHub` + SignalR-backed `IRealtimeNotifier` + ar/en request localization.** `src/ExecPlan.Api/Hubs/DashboardHub.cs` — `[Authorize]` `Hub` with `JoinActivation(Guid)`/`LeaveActivation(Guid)` over group `act-{id}`; `JoinActivation` runs the DEC-17 visibility check (Manager/Admin any; else caller must be a participant OR a TeamLeader of a participating team) and throws `HubException` (no group add) on failure. **GOTCHA:** identity is read from `Context.User` claims (`sub`/`ClaimTypes.Role`, matching `JwtTokenFactory`/`CurrentUser`) — NOT request-scoped `ICurrentUser`/`IHttpContextAccessor`, which is not reliably populated during hub method invocations; repo access via injected `IUnitOfWork`. `SignalRRealtimeNotifier : IRealtimeNotifier` (Api/Hubs) over `IHubContext<DashboardHub>` + `IDashboardService` — `DashboardChangedAsync` recomputes the snapshot and pushes `"DashboardUpdated"` (the `DashboardDto`) to the group; `ActivationClosedAsync` pushes `"ActivationClosed"` (the id). `Program.cs`: `RemoveAll<IRealtimeNotifier>()` + `AddScoped<IRealtimeNotifier, SignalRRealtimeNotifier>()` (after AddInfrastructure/AddApplication, replacing the no-op), `AddSignalR()`, `MapHub<DashboardHub>("/hubs/dashboard")`, JWT `OnMessageReceived` lifts `?access_token` onto `ctx.Token` for `/hubs/*` (WebSocket handshake auth), `AddLocalization()` + `RequestLocalizationOptions` (cultures ar/en, default ar, providers cookie→query→accept-language), `UseRequestLocalization()` before `MapControllers`. `LocalizationController` (`POST api/v1/set-language?culture=ar\|en` writes the `.AspNetCore.Culture` cookie via `CookieRequestCultureProvider.MakeCookieValue`; `GET api/v1/culture` `[AllowAnonymous]` diagnostic echoing `CultureInfo.CurrentCulture.Name`). **Realtime test approach:** REAL `Microsoft.AspNetCore.SignalR.Client` `HubConnection` (added pkg `9.0.*`) over the in-memory `TestAppFactory.Server` (long-polling transport via `CreateHandler()`, JWT via `AccessTokenProvider`) — member joins + acknowledges over HTTP and asserts the `DashboardUpdated` push (`ReadyCount==1`) within 10s; plus a `HubException` test for joining a non-viewable activation. (Long polling sends the token as an `Authorization` header; the query-string lift is production-WS-only and not exercised by the in-memory transport.) | `(this commit)` | 90 (18 unit unchanged; 72 integration: 68 prior + 4 new — 2 `RealtimeTests` [DashboardUpdated push, HubException on forbidden join], 2 `LocalizationTests` [default ar, set-language cookie→en]); build 0 warn/0 err |
| 2026-07-01 | T19 follow-up | **Security fix (Important, DEC-18): `DashboardHub.JoinActivation` no longer admits plain participants.** `src/ExecPlan.Api/Hubs/DashboardHub.cs`'s `CanViewAsync` previously let ANY participant of the activation join the group — including a plain `TeamMember` — so a member could receive the full cross-team `DashboardDto` over SignalR even though PRD §14 ("View live dashboard: Member –") and the REST `ActivationsController.Dashboard` gate (`[Authorize(Roles="SystemAdmin,PlanManager,TeamLeader")]` + leader-own check) both exclude members. Removed the "any participant" branch entirely; the gate now admits ONLY SystemAdmin/PlanManager, or a TeamLeader of a team participating in the activation — a plain TeamMember participant is rejected with `HubException`, matching the REST gate exactly. Docstring corrected (no longer claims "participant OR TeamLeader"). `tests/ExecPlan.IntegrationTests/RealtimeTests.cs` rewritten: the push test now has a PlanManager open the `HubConnection`/`JoinActivation` while a member acknowledges over HTTP (manager receives `DashboardUpdated`, `ReadyCount==1`); added a TeamLeader-of-a-participating-team positive join case; the participant-member case is now a negative (`HubException`) proving the §14 fix; kept the non-participant rejection case. | `(this commit)` | 92 (18 unit unchanged; 74 integration: 68 prior unchanged + 4 `RealtimeTests` [manager push, leader-can-join, member-participant-rejected, non-participant-rejected] + 2 `LocalizationTests` unchanged); build 0 warn/0 err |

| 2026-07-01 | T20 | **`ExecPlan.Cli` `run-escalation` entrypoint (FR-ESC-1: CLI and dashboard trigger identical behavior).** New `EscalationArgs` record (parses `--activation <guid>` xor `--all-active`, `null` on any malformed input) + `EscalationRunner` (process-free: takes only an `IServiceProvider`, creates one DI scope per activation, resolves `IEscalationService`/`IUnitOfWork` and writes `EscalationCycleResult` lines to a caller-supplied `TextWriter`; exit codes `ExitSuccess=0`/`ExitBadArgs=1`/`ExitError=2`). `Program.cs` now builds a real generic host (`Host.CreateApplicationBuilder` + `AddInfrastructure`+`AddApplication`, `ConfigureContainer(... ValidateOnBuild=true, ValidateScopes=true)` so a broken registration fails at startup) and dispatches `run-escalation` to `EscalationRunner`. **Cross-task wiring:** registered a trivial `NoOpCurrentUser : ICurrentUser` (always-null) in the CLI host only — `AddApplication` also registers `ExecutionService`/`BroadcastService`, which require `ICurrentUser` in their constructor even though `run-escalation` never resolves them; without it `ValidateOnBuild` would fail the whole host at startup. Added `appsettings.json` (placeholders, committed) + `CopyToOutputDirectory`. New `tests/ExecPlan.IntegrationTests/CliEscalationTests.cs` builds the SAME kind of container (`AddInfrastructure`+`AddApplication` rebound onto the shared `SqliteFixture` connection) and proves the CLI path is byte-for-byte identical to calling `IEscalationService.RunCycleAsync` directly: matching attempt counts on a structurally-identical parallel scenario, a full induction case driven entirely through `EscalationRunner`, `--all-active` iterating multiple Active activations, an unknown-activation error exit code, and `EscalationArgs.Parse` edge cases (both flags, neither flag, bad guid, unknown flag). | `1bb54bf` | 97 (18 unit unchanged; 79 integration: 74 prior + 5 new `CliEscalationTests`); build 0 warn/0 err |
| 2026-07-01 | T21 | **Idempotent dev/eval `DataSeeder` (DEC-19) + gated startup wiring.** `src/ExecPlan.Infrastructure/Seed/DataSeeder.cs` — `SeedAsync(IServiceProvider, ct)` guards on "any `User` row already exists" (no-op otherwise); seeds one Organization (+2 Departments), one user per role with KNOWN usernames (`admin`/`manager`/`leader`/`member`, plus a 5th `substitute` account) all sharing `DataSeeder.DemoPassword="Passw0rd!"` hashed via the real `IPasswordHasher`; a showcase storm-response `Plan` (PRD §1's "ideal first case", Status=Ready, created by `manager`) with `manager` ALSO registered as an explicit `PlanActivator`, two `Team`s (`leader` leads one), task templates per team, and an on-duty `ShiftAssignment` roster (`leader`+`member`) plus a frozen `substitute` row (`SubstituteForUserId=member`) written against WHATEVER Kuwait shift band/roster-date `IClock`+`KuwaitShiftCalculator` resolve to AT SEED TIME — so the plan activates cleanly on first boot regardless of time of day, not just at a hardcoded date. `ExecPlan.Api/Program.cs` runs `DataSeeder.SeedAsync(app.Services)` right after `builder.Build()`, gated on `app.Environment.IsDevelopment() || Configuration.GetValue<bool>("Seed:Enabled")` — confirmed this gate does NOT disturb `TestAppFactory`-hosted tests (its own `SeedAdmin` already inserts the `admin` user during `ConfigureServices`, before `Build()`, so `DataSeeder`'s idempotency guard fires first and the seeder is a no-op there). New `tests/ExecPlan.IntegrationTests/SeedTests.cs` (each test builds its OWN private `SqliteFixture` — the fixed, known usernames make sharing one across tests order-dependent): one-per-role + real-hash assertions, a real `IAuthService.LoginAsync` round-trip as `manager` (+ wrong-password→`AppException`), the showcase plan/teams/templates/roster/substitute shape, calling `SeedAsync` twice→no duplicates, and activating the seeded plan end-to-end (2 participants, the on-duty `member`'s `ResolvedSubstituteUserId` resolves to the seeded substitute, fresh tasks generated). **Per the build instructions for this task, the actual migration-apply-to-the-real-database + manual smoke-run (brief step 4) was deliberately NOT executed by this agent** — only build+SQLite-test verification; that step remains for the controller/human to run manually (`dotnet ef database update` then `dotnet run --project src/ExecPlan.Api` against the real connection string). | `(this commit)` | 102 (18 unit unchanged; 84 integration: 79 prior + 5 new `SeedTests`); build 0 warn/0 err |

## Phase 1 review

| 2026-07-01 | T22 | Whole-branch review (opus) ✅ READY TO MERGE — all invariants verified, PRD §21 met. Cleanups: removed dead IUnitOfWork.BeginTransactionAsync; spec CurrentShift→Resolve; logged tzdata + tenancy deploy notes | (cleanup) | 102/102 |

## Phase 2 — Web (MVC admin) — COMPLETE

| Date | Task | Done | Commit | Tests |
|---|---|---|---|---|
| 2026-07-01 | Task 1 | **Vendor self-hosted front-end assets + theme CSS + static-file serving.** Committed local Bootstrap 5.3.3 (`bootstrap.min.css`/`bootstrap.rtl.min.css`/`bootstrap.bundle.min.js`), `@microsoft/signalr@8.0.7/signalr.min.js`, and IBM Plex Sans Arabic 400/500/600 `.woff2` subsets under `src/ExecPlan.Api/wwwroot/{lib,fonts}` (no NuGet packages added — static files ship in `Microsoft.NET.Sdk.Web`; no CDN referenced at runtime per NFR-6). Wrote `wwwroot/css/execplan.css` (navy/amber design tokens, `.ep-navbar`/`.ep-sidebar`/`.ep-card`/`.btn-launch`/`.badge-*`/`.counter-tile`/`.ep-primary`, `@font-face` for the 3 weights). Added `app.UseStaticFiles();` to `Program.cs` right after `AppExceptionMiddleware`. New `tests/ExecPlan.IntegrationTests/Web/StaticAssetsTests.cs` (reuses the existing `TestAppFactory`) — theory over the 6 asset URLs, each asserted `200 OK`. | `(this commit)` | 108 (18 unit unchanged; 90 integration: 84 prior + 6 new `StaticAssetsTests`); build 0 warn/0 err |
| 2026-07-01 | Task 2 | **MVC shell — area routing, shared localized layout, login page (GET).** `Program.cs`: `AddControllersWithViews()` now chains `.AddViewLocalization().AddDataAnnotationsLocalization(...)` (localizer provider → `SharedResource`); the `AdminCookie` registration extended with `AccessDeniedPath=/admin/denied`, `ExpireTimeSpan=8h`, `SlidingExpiration=true`, `Cookie.HttpOnly=true`, `Cookie.SecurePolicy=Always`, `Cookie.SameSite=Lax`; after `MapControllers()` added the `adminArea` conventional fallback route (`{area:exists}/{controller=Home}/{action=Index}/{id?}`) and a `GET /` → `/admin` redirect. New `Resources/SharedResource.cs` (marker) + `SharedResource.ar.resx`/`.en.resx`, both populated with the full locked-contracts 66-key catalogue (ar=Arabic incl. the 4 fixed `Wizard.Step1..4` strings, en=English; `Lang.Toggle` shows the target language's own name — "English" in ar.resx, "العربية" in en.resx). Root + `Areas/Admin` each get `_ViewImports.cshtml` (`IViewLocalizer`, tag helpers) + `_ViewStart.cshtml` (`Layout="_Layout"` — area views resolve it via the standard `/Views/Shared/` fallback, no duplicate layout needed). `Views/Shared/_Layout.cshtml` sets `lang`/`dir` from `CultureInfo.CurrentUICulture`, links RTL/LTR Bootstrap + `execplan.css`, renders the localized navbar/sidebar/language-toggle form (posts `/admin/language`) and logout form (posts `/admin/logout`) — both endpoints deliberately NOT built yet (Tasks 5/3). `AccountController` (`[Area("Admin")][Route("admin")]`) — `GET login` (redirects `/admin` if already authenticated, else renders `LoginVm`), `GET denied`. `Login.cshtml`/`Denied.cshtml` + `LoginVm{UserName,Password,ReturnUrl,Error}`. Minimal `HomeController` (`[Authorize(AuthenticationSchemes=AdminCookieScheme)]`, `GET ""` → redirect `/admin/plans`) so `/admin` demands auth. | `(this commit)` | 111 (18 unit unchanged; 93 integration: 90 prior + 3 new `ShellTests` — root redirect, ar/RTL login render, unauthenticated `/admin`→login redirect); build 0 warn/0 err |

| 2026-07-01 | Task 3 | **Cookie sign-in — login POST, logout, role landing, Member rejection.** `AdminClaimsPrincipalFactory.Create(AppUserPrincipal)` builds the cookie identity with the exact claims `CurrentUser` reads (`Sub`, `ClaimTypes.Role`, `Name`). `AccountController`: `POST login` validates via `IAuthService.ValidateCredentialsAsync`, rejects `TeamMember` (no web surface), signs into `AdminCookie`, `LocalRedirect` with open-redirect guard (`Url.IsLocalUrl`); `POST logout` `[Authorize(AdminCookie)]`+`[ValidateAntiForgeryToken]` → `SignOutAsync`. `[AllowAnonymous]` moved from the CLASS to the individual Login/Denied actions so Logout's `[Authorize]` is actually enforced (a controller-level `[AllowAnonymous]` would otherwise win). `HomeController.Index` role-lands Admin/Manager→`/admin/plans`, Leader→`/admin/activations`. **Localization fix:** both `_ViewImports.cshtml` switched `@inject IViewLocalizer` → `@inject IStringLocalizer<SharedResource>` — `IViewLocalizer` resolves by the executing view's path, so with a single shared resx every `@Localizer["Key"]` was silently falling back to the raw key; `IStringLocalizer<SharedResource>` binds the shared resource correctly. `_Layout` logout form got `@Html.AntiForgeryToken()`. New `tests/.../Web/WebTestHelpers.cs` (`NewClient` = https-base + cookie + no-redirect so the Secure `AdminCookie` round-trips under TestServer; `LoginAsync`/`PostFormAsync` w/ anti-forgery scrape) and `AuthFlowTests.cs` (5 tests: manager lands on plans, bad-creds generic error + no cookie, member rejected, logout clears cookie via minted identity-bound antiforgery pair, open-redirect blocked). | `(this commit)` | 116 (18 unit; 98 integration: 93 prior + 5 new `AuthFlowTests`); build 0 warn/0 err |

| 2026-07-01 | Task 4 review fix | **Coverage backfill:** the Task 4 review flagged that 2 of the 5 `AppException.Kind` HTML branches (Unauthorized, Validation) shipped without a test. Added `Html_unauthorized_redirects_to_login` (GET `/admin/_throw/unauthorized` → 302 `Location` starts `/admin/login?returnUrl=`) and `Html_validation_shows_message` (GET `/admin/_throw/validation` → 302 `/admin/error?msg=…`, then follow it → decoded body contains the echoed `ex.Message`) to `ErrorMappingTests.cs`. Production code untouched — both branches were already correct. | `(this commit)` | 122 (18 unit unchanged; 104 integration: 102 prior + 2 new); build 0 warn/0 err |
| 2026-07-01 | Task 4 | **`AppExceptionMiddleware` content negotiation (HTML views vs API JSON) + shared error views.** `WantsJson(ctx)` = path starts with `/api` OR `Accept` names `application/json` without also naming `text/html`; the `catch (AppException ex)` branch is now `if (WantsJson) { <the unchanged Phase-1 JSON body>; return; }` else `RedirectHtml` by `ex.ErrorKind`: Unauthorized→`/admin/login?returnUrl=<escaped path>`, Forbidden→`/admin/denied` (Task 3's `Account.Denied`), NotFound→`/admin/notfound`, Validation/Conflict→`/admin/error?msg=<escaped message>`. The `catch (Exception ex)` (non-AppException) branch keeps its JSON body unchanged for `WantsJson`; for HTML it redirects to `/admin/error` with **no** msg (never leaks the exception message/stack trace — unlike the Validation/Conflict AppException case, which is a deliberately user-facing message). New `ErrorPageController` (`[Area("Admin")][Route("admin")][AllowAnonymous]`) — `GET notfound`/`GET error`, each sets the real status code (404/400) + `ViewBag.Msg` and returns `View("NotFound"/"Error")`; resolved via MVC's area-view-location fallback to top-level `Views/Shared/NotFound.cshtml`/`Error.cshtml` (both a `.ep-card` showing `@Localizer["Error.NotFound"/"Error.Generic"]` + optional `@ViewBag.Msg` + a `Common.Back` link to `/admin` — no `_ViewImports`/`_ViewStart` changes needed, the top-level ones already inject `IStringLocalizer<SharedResource>`/set `Layout="_Layout"`). **Test-only `ThrowController`** (`admin/_throw/{kind}`, `[AllowAnonymous]`) throws the matching `AppException.Kind` (or a plain `ArgumentOutOfRangeException` for an unknown kind, to test the non-AppException HTML fallback) — gated on `IWebHostEnvironment.IsDevelopment()` (returns 404 otherwise); confirmed empirically (throwaway probe test, removed after) that `TestAppFactory`'s hosted `Program` runs with `EnvironmentName=Development`, same gate a real Production deployment would fail, so this route never exists there. New `tests/.../Web/ErrorMappingTests.cs` (4 tests: HTML NotFound→404 view containing the Arabic `Error.NotFound` string, HTML Forbidden→302 `/admin/denied`, HTML unhandled-non-AppException→400 generic-error view with no exception text, and — reusing a genuine Phase 1 endpoint, `GET /api/v1/activations/{unknownId}/dashboard` as the seeded admin — API `/api/*` + `Accept: application/json` still returns the unchanged `{error,kind}` JSON body, not HTML). **RED/GREEN verified via `git stash`:** stashing just the middleware edit (keeping the new controllers/views/tests) reproduced 3/4 failures against the old JSON-only middleware (the JSON test still passed, proving that branch was always fine); restoring the fix turned all 4 green. | `(this commit)` | 120 (18 unit unchanged; 102 integration: 98 prior + 4 new `ErrorMappingTests`); build 0 warn/0 err |
| 2026-07-01 | Task 5 | **Language toggle (ar/en culture cookie).** New `LanguageController` (`[Area("Admin")][AllowAnonymous][Route("admin")]`) — `POST language` (`[ValidateAntiForgeryToken]`) whitelists `culture` to `ar`/`en` (anything else falls back to `ar`), writes the `.AspNetCore.Culture` cookie via `CookieRequestCultureProvider.MakeCookieValue` (the cookie provider registered first in `Program.cs`'s `RequestLocalizationOptions`, so it alone drives the resolved culture on the next request), and `LocalRedirect`s to a guarded local `returnUrl` (`Url.IsLocalUrl`, else `/admin/login`). `_Layout`'s language-toggle form (built in Task 2, previously missing a token since only the logout form got one in Task 3) now has `@Html.AntiForgeryToken()` too. New `tests/.../Web/LocalizationTests.cs` (3 tests, both toggle directions asserted against the REAL resx strings per Task 3's `IStringLocalizer<SharedResource>` fix, not just `dir`/`lang`): default GET `/admin/login` is `dir="rtl"`/`lang="ar"` + contains the Arabic `Login.Title`; POST `culture=en` → 302 + `.AspNetCore.Culture` Set-Cookie, follow-up GET is `dir="ltr"`/`lang="en"` + contains the English `Login.Title`; POST `culture=zz` → next page still Arabic/RTL (whitelist fallback). RED confirmed first (404 — no `/admin/language` route yet) before adding the controller. | `(this commit)` | 125 (18 unit unchanged; 107 integration: 104 prior + 3 new `LocalizationTests` [Web]); build 0 warn/0 err |
| 2026-07-01 | Task 6 | **Users administration (Index/Create/Edit; Admin write, Manager read-only).** New `Areas/Admin/Controllers/UsersController.cs` (`[Route("admin/users")]`, class gate `AdminCookieScheme`+`ManagerOrAdmin`): `GET ""` builds `UserListVm` (`CanWrite = User.IsInRole(SystemAdmin)`, rows join each user's `DepartmentId` against `Repo<Department>().ListAsync()` for a display name); `GET/POST "create"` and `GET/POST "{id:guid}/edit"` add `[Authorize(Policy=Admin)]` on top of the class policy. Create hashes the password via `IPasswordHasher.Hash`; Edit loads the tracked `User` via `GetByIdAsync` (already change-tracked — no explicit re-attach needed), overwrites `FullName/Phone/Role/OrganizationId/DepartmentId/IsActive`, re-hashes the password only if a new one was supplied, and **never deletes** — deactivate is just `IsActive=false`. Both writes end in exactly one `SaveChangesAsync`. `BuildEditVmAsync`/`FillListsAsync` populate `SelectList`s over `Repo<Organization>`/`Repo<Department>`. New VMs `UserListVm{Row(Id,UserName,FullName,Role,Department,IsActive), CanWrite}`, `UserEditVm{Id?,UserName,FullName,Phone,Role,OrganizationId,DepartmentId?,Password?,IsActive,Orgs,Depts}`. 3 views (`Index`/`Create`/`Edit.cshtml`): Index hides both the `Users.Add` link and the per-row Edit link when `!CanWrite`; Create/Edit are plain `name=`-bound forms (`@Html.AntiForgeryToken()`, Role/Org/Dept `<select>`s) matching this codebase's existing convention (no `asp-for`). Extended both resx files with `Users.Organization`/`Users.Department`/`Users.NoDepartment` + `Role.SystemAdmin`/`PlanManager`/`TeamLeader`/`TeamMember` (ar+en), per locked-contracts' "extend as noted per task". **Gotcha hit + fixed:** Edit's `IsActive` checkbox has no `asp-for`, so unchecking it posts no field at all — with `UserEditVm.IsActive` defaulting `true` (needed so a fresh Create-vm defaults active), a naive unchecked-checkbox would silently leave the bound vm at `true` instead of `false`. Fixed by rendering a hidden `name="IsActive" value="false"` fallback **after** the checkbox in the DOM (same trick `asp-for`'s checkbox tag helper uses): `SimpleTypeModelBinder` binds a bool from the *first* posted value for a repeated key, so checked→"true" wins, unchecked→only "false" posts. New `tests/.../Web/UsersAdminTests.cs` (4 tests, seeds its own Manager + Organization idempotently, same pattern as `AuthFlowTests`): admin-creates-persists-with-hashed-password (`IPasswordHasher.Verify` true, raw hash ≠ plaintext); manager GET list is 200 and the body lacks `/admin/users/create`; manager POST create → 302 containing `/admin/denied` (real `AccessDeniedPath`, not a bare 403) + row never persisted; admin edits a seeded user posting the unchecked-checkbox wire shape (`IsActive=false`) → repo shows `IsActive=false`. RED confirmed first (all 4 failed 404 — no route yet); GREEN required 2 fixes beyond the initial pass: the denied-redirect assertion needed `Contain` not `Be` (real `Location` is an absolute URL + `?ReturnUrl=...`), and the deactivate test needed to literally post `IsActive=false` (a Dictionary-backed form helper can't represent two same-key values, so it simulates the hidden-fallback's wire value directly rather than "field omitted"). | `(this commit)` | 129 (18 unit unchanged; 111 integration: 107 prior + 4 new `UsersAdminTests`); build 0 warn/0 err |
| 2026-07-01 | Task 7 | **Departments + Organizations administration (Index/Create; Admin write, Manager read-only). Organizations screen satisfies FR-ADM-1 (DEC-25), which PRD §16's screen list omitted.** New `Areas/Admin/Controllers/OrganizationsController.cs` (`[Route("admin/organizations")]`) and `DepartmentsController.cs` (`[Route("admin/departments")]`), both mirroring `UsersController`'s shape exactly: class gate `AdminCookieScheme`+`ManagerOrAdmin`, `GET ""` builds an Index VM (`CanWrite = User.IsInRole(SystemAdmin)` + a rows list — `DeptListVm.Row` joins each department's `OrganizationId` against `Repo<Organization>().ListAsync()` for a display name), `GET/POST "create"` adds `[Authorize(Policy=Admin)]` on top of the class policy, one `SaveChangesAsync` per write, `AddAsync` (no tracked-collection mutation), Guid PKs ctor-assigned. `DepartmentsController.Create`'s VM carries a `SelectList Orgs` (`BuildVmAsync` populates it over `Repo<Organization>`) for the required Organization `<select>`. New VMs `OrgVm{Id?,Name}`/`OrgListVm{Row(Id,Name),CanWrite}` and `DeptVm{Id?,Name,OrganizationId,Orgs}`/`DeptListVm{Row(Id,Name,Organization),CanWrite}`. 4 views (`Views/Organizations/{Index,Create}.cshtml`, `Views/Departments/{Index,Create}.cshtml`) mirror the Users Index/Create shape (`ep-card` table, `ep-primary` Add link gated on `CanWrite`, `@Html.AntiForgeryToken()` forms); Departments' table/form reuse the existing `Users.Organization` resx key for the org column/label rather than adding a new one (all `Dept.*`/`Org.*`/`Common.*` keys needed already existed in `SharedResource.{ar,en}.resx` from Task 2 — no resx changes this task). New `tests/.../Web/OrgDeptAdminTests.cs` (3 tests, seeds its own Manager + Organization idempotently, same pattern as `UsersAdminTests`): admin creates an organization → persisted; admin creates a department under an org → persisted with the correct `OrganizationId`; manager GET `/admin/departments` is 200 with no `/admin/departments/create` link in the body, and manager POST create → 302 containing `/admin/denied` (cookie authz failure, not a bare 403) + row never persisted. | `(this commit)` | 132 (18 unit unchanged; 114 integration: 111 prior + 3 new `OrgDeptAdminTests`); build 0 warn/0 err |
| 2026-07-01 | Task 8 | **"My Plans" list + read-only plan detail (Index/Detail only — the Create-Plan wizard is Tasks 9-12, Activate is Task 13).** New `Areas/Admin/Controllers/PlansController.cs` (`[Route("admin/plans")]`, class gate `AdminCookieScheme`+`ManagerOrAdmin`): `GET ""` scopes `Plan` rows to `CreatedByUserId == ICurrentUser.UserId` for a manager, or every plan for `User.IsInRole(SystemAdmin)`; each row's `ActiveActivationId` is the id of any `PlanActivation` for that plan with `Status==Active` (from one `ListAsync(a => a.Status==Active)` joined in memory), driving a "watch dashboard" link to `/admin/activations/{id}`. `GET "{id:guid}"` loads the `Plan` (missing → `throw AppException.NotFound(...)`, which `AppExceptionMiddleware` redirects to `/admin/notfound`) and assembles `TeamBlock`s via **separate `ListAsync` calls per collection** (`Team` by `PlanId` → `TeamMembership` by `TeamId IN (...)` → `User` by `Id IN (...)` for full names → `TaskTemplate` by `TeamId IN (...)`, ordered by `Order`), joined in memory — no EF `.Include()` anywhere in the Api layer, per this task's global constraint. New VMs `PlanListVm{Plans:[Row(Id,Name,Type,Status,ActiveActivationId?)]}` and `PlanDetailVm{Id,Name,Type,Status,Objective?,Description?,Scope?,Teams:[TeamBlock(Name,Members,Tasks)],ActiveActivationId?}`. `Views/Plans/Index.cshtml` (`ep-card` table, localized `Plans.Type`/`Status.Draft`\|`Status.Ready` badge (`badge-draft`/`badge-ready`), top `Plans.New` → `/admin/plans/create`) and `Detail.cshtml` (plan info `dl`, team/member/task read-back cards; the exact placeholder comment `<!-- activate: Task 13 -->` marks where the Activate button goes). Extended both resx files with `Plans.Status`/`Objective`/`Description`/`Scope`/`Teams`/`Members`/`Tasks`/`NoTeams`/`NoPlans`/`WatchDashboard` + `PlanType.{Daily,Weekly,Emergency,Guard,Transport,Maintenance,It,Inspection,General}` (ar+en) — none of these existed yet in the Task 2 catalogue. New `tests/.../Web/PlansListDetailTests.cs` (4 tests, seeds its own Manager + Organization idempotently): manager sees only their own plan + its status badge, not another creator's plan; admin sees both a manager-created and an admin-created plan; Detail renders a seeded Team's name + member full name + task title; an unknown plan id GETs a 302 to `/admin/notfound` (matches the Task 4 middleware's redirect contract, not a bare 404). | `(this commit)` | 136 (18 unit unchanged; 118 integration: 114 prior + 4 new `PlansListDetailTests`); build 0 warn/0 err |
| 2026-07-01 | Task 9 | **Create-plan wizard step 1 (plan info -> Draft) — first of the 4-step server-incremental wizard (Tasks 10-12 add teams/tasks/review onto the same `{id}`).** New `Areas/Admin/Controllers/PlanWizardController.cs` (`[Route("admin/plans/create")]`, class gate `AdminCookieScheme`+`ManagerOrAdmin`): `GET ""` sets `ViewData["step"]=1` and renders a blank `WizardInfoVm`; `POST ""` (`[ValidateAntiForgeryToken]`) requires `Name` (else `ModelState` error + step 1 re-render), else creates `Plan{Status=Draft, CreatedByUserId=ICurrentUser.UserId}` **plus** a `PlanActivator{PlanId,UserId=creator}` — the creator is an implicit authorized activator, mirroring the seeded-manager pattern from Task 21's `DataSeeder` — both via `Repo<T>().AddAsync` (no tracked-collection mutation), one `SaveChangesAsync`, then redirects to `/admin/plans/create/{id}/teams` (Task 10's not-yet-built route — a resumable, refresh-safe step boundary). New VM `WizardInfoVm{Name,Type,Objective?,Description?,Scope?}` (non-nullable `Plan.Objective/Description/Scope` are coalesced from the nullable VM fields via `?? ""`, matching the `UsersController` convention, to keep the build warning-free under `Nullable=enable`). New `Views/PlanWizard/_Steps.cshtml` — a shared partial (reused by Tasks 10-12) rendering the four localized `Wizard.Step1..4` names from `ViewData["step"]`, highlighting the active one; `Info.cshtml` renders `_Steps` + a plain `name=`-bound form (Name required, Type `<select>` over `PlanType`, Objective/Description/Scope textareas, `@Html.AntiForgeryToken()`, one `ep-primary` `Wizard.Next` submit) matching the existing Users/Orgs/Depts form convention (no `asp-for`). All resx keys needed (`Wizard.*`, `Plans.Name`/`Type`/`Objective`/`Description`/`Scope`, `PlanType.*`) already existed from Tasks 2/8 — no resx changes this task. No `Domain`/`Application` changes — `Plan`/`PlanActivator`/`PlanType`/`PlanStatus`/`ICurrentUser` all pre-existed. New `tests/.../Web/WizardStep1Tests.cs` (2 tests, seeds its own Manager + Organization idempotently, same pattern as `PlansListDetailTests`): POST with Name+Type → 302 to `/admin/plans/create/{id}/teams`, then in a fresh scope asserts the `Plan` row (`Status=Draft`, `CreatedByUserId`=manager, `Name`/`Type` persisted) and a `PlanActivator` row for that manager on that plan both exist; POST with no `Name` → 200, body still contains `admin/plans/create` (step 1 re-rendered, not a redirect). Both passed on the first run (no red/green cycle needed — implementation matched the brief's pseudocode exactly). | `(this commit)` | 138 (18 unit unchanged; 120 integration: 118 prior + 2 new `WizardStep1Tests`); build 0 warn/0 err |
| 2026-07-01 | Task 10 | **Create-plan wizard step 2 (teams & members) — adds a reusable ownership guard steps 2-4 will all call.** `PlanWizardController.LoadDraftForEditAsync(id, ct)` loads the `Plan` **tracked** (`FirstOrDefaultTrackedAsync`, so later steps can mutate it and rely on one later `SaveChangesAsync`): missing → `throw AppException.NotFound` (→ `/admin/notfound`); `Status != Draft` → plain `Redirect($"/admin/plans/{id}")` (already finalized — "nothing left to wizard here", not a Forbidden); owner mismatch (`CreatedByUserId != ICurrentUser.UserId` and not `User.IsInRole(SystemAdmin)`) → `throw AppException.Forbidden` (→ `/admin/denied`, cookie-authz style, not a bare 403). Returns `(Plan? Plan, IActionResult? ShortCircuit)`; every step action checks `ShortCircuit` first. `GET {id}/teams` builds `WizardTeamsVm` from the draft's current DB rows (`Team` by `PlanId` → `TeamMembership` by `TeamId IN (...)` → every `User` for the leader/member pickers), `ViewData["step"]=2`. `POST {id}/teams` (`[ValidateAntiForgeryToken]`) reads `intent` off two sibling `<form>`s in the view: `intent=add` validates `Name` non-empty (else `ModelState` error + re-render), then adds one `Team` + one `TeamMembership` per selected member **each via `Repo<T>().AddAsync`** (never mutating a tracked collection nav), one `SaveChangesAsync`, then re-renders step 2 with the new team now listed — posting "add" again layers on another team across multiple round trips; `intent=next` re-queries the draft's teams+memberships and only advances (`Redirect .../tasks`) once at least one team has at least one member — enforced **server-side** so a crafted/replayed request can't skip it — otherwise re-renders step 2 with `ViewData["teamsBlocked"]` = the new `Teams.AtLeastOneRequired` resx message. New VMs (`WizardTeamsVm.cs`) `WizardTeamsVm{PlanId, Teams:List<TeamInput>, AllUsers:IReadOnlyList<UserOption>}`, `TeamInput{Name, TeamLeaderUserId?, MemberUserIds:List<Guid>}` (reused for both the persisted-teams read-back and the add-team form's bound input — same shape either way), `UserOption{Id, Label}`. New `Views/PlanWizard/Teams.cshtml`: `_Steps` (step 2), existing-teams cards (name + optional leader + member names looked up from `AllUsers`, `Plans.NoTeams` when empty), an alert for `teamsBlocked`, the add-team form (`Name` input, `TeamLeaderUserId` `<select>` with a "no leader" placeholder, `MemberUserIds` `<select multiple>`, hidden `intent=add`, `ep-primary` `Teams.Add` submit), and a second small form (hidden `intent=next`, `ep-primary` `Wizard.Next` submit). Extended both resx files with `Teams.Name`/`Leader`/`NoLeader`/`Add`/`AtLeastOneRequired` (ar+en) — everything else reused existing `Plans.*`/`Wizard.*` keys. **Test-helper gap closed:** a multi-select posts the SAME field name (`MemberUserIds`) multiple times, which the existing `WebTestHelpers.PostFormAsync(HttpClient,string,string,IDictionary<string,string>)` cannot represent (last-key-wins silently drops all but one selection). Added a second overload taking `IEnumerable<KeyValuePair<string,string>>` (ordered list, duplicates allowed) that scrapes the antiforgery token the same way and builds `FormUrlEncodedContent` from the raw pair list — C#'s better-conversion-target overload resolution still picks the `IDictionary` overload for existing `Dictionary`-based call sites (no call-site changes needed); Tasks 11-12 will reuse the new overload for their own repeated-field posts. New `tests/.../Web/WizardStep2Tests.cs` (3 tests, seeds Manager + a SECOND manager (foreign-draft case) + 2 plain members idempotently; each test seeds its own Draft `Plan` directly rather than driving Step 1): `Add_team_with_members_persists` posts `intent=add` + a 2-member `MemberUserIds` list via the new repeated-key helper → 200 re-render containing the team name, then in a fresh scope asserts the `Team` (with `TeamLeaderUserId`) and both `TeamMembership` rows exist; `Advancing_without_team_is_blocked` posts `intent=next` with no team yet → 200 (re-render, not a redirect to `/tasks`), `Plan.Status` still `Draft`; `Foreign_draft_forbidden` has manager2 GET manager1's draft's teams page → 302 `/admin/denied`. All 3 passed against the guard+actions as first written (no red/green cycle needed). | `(this commit)` | 141 (18 unit unchanged; 123 integration: 120 prior + 3 new `WizardStep2Tests`); build 0 warn/0 err |
| 2026-07-01 | Task 11 | **Create-plan wizard step 3 (tasks) — attaches `TaskTemplate` rows to each of the draft's teams, reusing Task 10's `LoadDraftForEditAsync` ownership guard unchanged.** `GET {id}/tasks` builds `WizardTasksVm` from the draft's current DB rows (`Team` by `PlanId` → `TaskTemplate` by `TeamId IN (...)`, grouped per team and ordered by `Order`), `ViewData["step"]=3`. `POST {id}/tasks` (`[ValidateAntiForgeryToken]`) reads `intent` off each team's own add-task form plus the page's shared "next" form: `intent=add` re-checks the posted `teamId` belongs to this draft's own teams (`Repo<Team>().FirstOrDefaultAsync(t => t.Id==teamId && t.PlanId==planId)` — a crafted `teamId` for someone else's plan/team → 404, not a silent cross-plan write), validates `Title` non-empty, then adds one `TaskTemplate{TeamId, Title, Order, Duration=TimeSpan.FromMinutes(DurationMinutes)}` via `Repo<TaskTemplate>().AddAsync`, one `SaveChangesAsync`, then re-renders step 3 with the new task now listed under its team — posting "add" again (same or a different team) layers on another task across multiple round trips; `intent=next` re-queries every `TaskTemplate` across the draft's teams and only advances (`Redirect .../review`, Task 12's not-yet-built route) once at least one exists — enforced **server-side** so a crafted/replayed request can't skip it — otherwise re-renders step 3 with `ViewData["tasksBlocked"]` = the new `Tasks.AtLeastOneRequired` resx message. New `WizardTasksVm.cs`: `WizardTasksVm{PlanId, Teams:IReadOnlyList<TeamTasks>}`, `TeamTasks{TeamId, TeamName, Tasks:List<TaskInput>}`, `TaskInput{Title, Order, DurationMinutes}` (reused for both the persisted-tasks read-back — `DurationMinutes=(int)Duration.TotalMinutes` — and each team's add-task form's bound input). New `Views/PlanWizard/Tasks.cshtml`: `_Steps` (step 3), one card per team (task list ordered by `Order` showing `"{Order}. {Title} ({DurationMinutes} min)"`, `Tasks.NoTasks` when empty, `Plans.NoTeams` if the draft somehow has no teams yet) each with its own add-task form (`Title`/`Order`/`DurationMinutes` inputs, hidden `intent=add` + hidden `TeamId`, `Tasks.Add` submit), an alert for `tasksBlocked`, and a final small form (hidden `intent=next`, `ep-primary` `Wizard.Next` submit). Extended both resx files with `Tasks.Title`/`Order`/`DurationMinutes`/`MinutesShort`/`Add`/`NoTasks`/`AtLeastOneRequired` (ar+en); `Plans.Tasks`/`Plans.NoTeams`/`Wizard.*` all already existed from Tasks 2/8/9. No `Domain`/`Application` changes — `TaskTemplate`/`Team`/`IUnitOfWork` all pre-existed. New `tests/.../Web/WizardStep3Tests.cs` (2 tests, seeds its own Manager + Organization + one plain member idempotently; each test seeds its own Draft `Plan` **with one `Team` and a `TeamMembership`** directly, bypassing steps 1-2): `Add_task_persists_with_duration` posts `intent=add` + `TeamId`/`Title`/`Order=1`/`DurationMinutes=30` → 200 re-render containing the title, then in a fresh scope asserts the `TaskTemplate` row exists with `Order=1` and `Duration==TimeSpan.FromMinutes(30)`; `Advancing_without_any_task_blocked` posts `intent=next` against a draft whose only team has no tasks yet → 200 (re-render, not a redirect to `/review`), `Plan.Status` still `Draft`. Both passed against the guard+actions as first written (no red/green cycle needed). | `(this commit)` | 143 (18 unit unchanged; 125 integration: 123 prior + 2 new `WizardStep3Tests`); build 0 warn/0 err |
| 2026-07-01 | Task 12 | **Create-plan wizard step 4 (shifts & review -> Ready) — the wizard's final step — plus a `RequireStep` navigation guard retrofitted onto every earlier step's GET.** New `PlanWizardController.RequireStep(planId, step, ct)`: given the requested step number, re-checks every EARLIER step's completion prerequisite for this draft and returns a redirect to the earliest unmet one's URL if not met (null when everything up to `step` holds). Step 2's only prerequisite is "still a Draft" — already enforced by `LoadDraftForEditAsync` — so requesting step 2 is a no-op; step ≥3 re-checks "≥1 team with ≥1 member" (else redirect `.../teams`) and step ≥4 additionally re-checks "≥1 team with ≥1 task" (else redirect `.../tasks`) — the exact same rules the Teams/Tasks POST "next" intents already enforce, now also blocking a crafted/replayed **GET** deep-link. Retrofitted one `RequireStep` call, right after `LoadDraftForEditAsync`, into the existing `Teams` GET (step 2, effectively a no-op) and `Tasks` GET (step 3) — both existing `WizardStep2Tests`/`WizardStep3Tests` (5 tests) still pass unchanged, confirmed by a full test run. **`Review` GET** (`{id}/review`) builds `WizardReviewVm` via `BuildReviewVmAsync`: one default `RosterInput{TeamId,UserId,Shift=Morning,Date=today}` row per existing `TeamMembership` of the draft (the roster editor's starting point) plus `ReviewReadback` — a read-only projection of the whole plan (info + teams/members/tasks), the same team/membership/task lookups `PlansController.Detail` uses. **`Review` POST = Finish** (`List<RosterInput> roster` bound via `roster[i].Property` indexed form keys): validates FIRST, before anything touches the change tracker — roster must be non-empty AND every `SubstituteForUserId` must reference a `TeamMembership` user of this plan's teams — else rebuilds the VM (keeping the caller's just-submitted `Roster` for correction), sets `ViewData["reviewBlocked"]` to the new `Review.RosterInvalid` resx message, and re-renders step 4 (200, nothing persisted); once valid, stages one `ShiftAssignment` per roster row via `Repo<ShiftAssignment>().AddAsync`, flips the tracked `Plan.Status` to `Ready`, and commits both in the single `SaveChangesAsync` call (roster + status flip atomic — a reader can never observe one without the other) before redirecting to `/admin/plans/{id}`. New `WizardReviewVm.cs`: `WizardReviewVm{PlanId, Roster:List<RosterInput>, Readback:ReviewReadback}`, `RosterInput{TeamId,UserId,Shift:ShiftBand,Date,SubstituteForUserId?}`, `ReviewReadback{PlanName,Type,Objective?,Description?,Scope?,Teams:IReadOnlyList<TeamBlock>}` with nested `TeamBlock(TeamId,Name,Members:IReadOnlyList<MemberRow>,Tasks)`/`MemberRow(UserId,Label)`. New `Views/PlanWizard/Review.cshtml`: `_Steps` (step 4), an alert for `reviewBlocked`, a roster `<table>` (one row per `Roster` entry: team/member name looked up by id, `Shift` `<select>` over `ShiftBand` via `Localizer["Shift."+s]`, a `date` input, an optional `Substitute` `<select>` over every member of the plan) with hidden `TeamId`/`UserId` inputs and one `ep-primary` `Wizard.Finish` submit, then the read-back (plan info `dl`, per-team member/task cards) mirroring `Plans/Detail.cshtml`'s layout. Extended both resx files with `Review.Roster`/`PlanSummary`/`Shift`/`Date`/`Substitute`/`NoSubstitute`/`RosterInvalid` + `Shift.Morning`/`Evening`/`Night` (ar+en) — `Plans.Name`/`Type`/`Objective`/`Description`/`Scope`/`Teams`/`Members`/`Tasks`/`Wizard.Finish` all reused from Tasks 2/8/9. No `Domain`/`Application` changes — `ShiftAssignment`/`ShiftBand`/`PlanStatus.Ready` all pre-existed. New `tests/.../Web/WizardStep4Tests.cs` (3 tests, seeds its own Manager + Organization + 2 members idempotently): `Finish_sets_plan_ready_and_persists_roster` seeds a fully-prepared Draft (team + 2 members + 1 task template) and posts a 2-row roster (one with a `SubstituteForUserId`) → 302 `/admin/plans/{id}`, then in a fresh scope asserts both `ShiftAssignment` rows exist with the right `Shift`/`SubstituteForUserId` and `Plan.Status==Ready`; `Empty_roster_blocks_finish` posts no roster rows against the same kind of ready draft → 200 re-render, `Plan.Status` still `Draft`, zero `ShiftAssignment` rows for that team; `Deeplink_to_review_before_prereqs_redirects_back` seeds a bare Draft with a team+member but **no** task and GETs `/review` directly → 302 to `.../tasks` (the earliest unmet step, proving `RequireStep`'s cumulative check). All 3 passed on the first run; full suite re-run afterward confirmed `WizardStep2Tests`/`WizardStep3Tests` unaffected by the retrofit. | `(this commit)` | 146 (18 unit unchanged; 128 integration: 125 prior + 3 new `WizardStep4Tests`); build 0 warn/0 err |
| 2026-07-01 | Task 13 | **Activate button on Plan Detail (oversized amber launch + confirm) — POST `/admin/plans/{id}/activate`.** `PlansController` now also injects `IActivationService`; `Activate(Guid id, ct)` calls `_activation.ActivateAsync(id, _currentUser.UserId!.Value, ct)` and, on success, `Redirect($"/admin/activations/{activationId}")` (Task 14's not-yet-built dashboard — a plain URL string is all this task needs). `AppException.Kind.NotFound`/`Forbidden` are deliberately **not caught** — they propagate to the existing `AppExceptionMiddleware`, which redirects to `/admin/notfound`/`/admin/denied` respectively (per the task's ambiguity-resolution: the Interfaces line + tests govern over the brief's Step-1 code sample, which would have let Forbidden/NotFound 500). Only `Kind.Conflict` (the common "no one is on duty for this shift"/"already active" case) and, defensively, `Kind.Validation` are caught: `TempData["activateError"] = ex.Message` then `Redirect($"/admin/plans/{id}")` back to Detail. `Detail.cshtml`'s `<!-- activate: Task 13 -->` placeholder is now: a `TempData["activateError"]` → `alert alert-danger` render; when `Status==Ready`, a `<form>` posting to `/admin/plans/{id}/activate` (`@Html.AntiForgeryToken()`, `onsubmit="return confirm(@Localizer["Plans.ActivateConfirm"])"`) with the single oversized `btn-launch` (the only amber button in the app) labeled `Plans.Activate`; when `Draft`, a muted hint paragraph (new resx key `Plans.ActivateDraftHint`, ar+en — the only two other referenced keys, `Plans.Activate`/`Plans.ActivateConfirm`, already existed in the catalogue). New `tests/.../Web/ActivateTests.cs` (3 tests, seeds 2 PlanManagers + 1 TeamMember + an Organization idempotently, same pattern as `PlansListDetailTests`): `Activate_ready_plan_redirects_to_dashboard` seeds a Ready plan + team + a `ShiftAssignment` aligned EXACTLY to `TestAppFactory.FixedShift` (Band+RosterDate, `SubstituteForUserId=null`) for the creator-manager → Detail page contains `btn-launch` → POST → 302 `/admin/activations/{guid}` → verifies the `PlanActivation` row (`Status=Active`, correct `PlanId`) in a fresh scope; `Activate_unauthorized_activator_denied` has a second manager (neither the plan's creator nor a `PlanActivator`) view Detail (200 — the class policy has no ownership check) then POST activate → service Forbidden → 302 `/admin/denied`; `Activate_draft_plan_shows_conflict` posts activate on a bare Draft plan with no roster at all (creator-authorized, so the service reaches the on-duty check and finds nothing) → 302 back to `/admin/plans/{id}`, follow-up GET renders `alert-danger` containing the literal service message. RED confirmed first (all 3 failed — 404/no button — before the controller/view changes). **Noted flake, not a regression:** one full-suite run hit a pre-existing `DependencyInjectionTests` race (documented in `TestAppFactory`'s own comments — multiple `IClassFixture<TestAppFactory>` classes mutate process-global `Database__Provider`/`Jwt__*` env vars in ctor/Dispose, which can interleave under xUnit's parallel class execution); two immediate re-runs were both fully green. | `(this commit)` | 149 (18 unit unchanged; 131 integration: 128 prior + 3 new `ActivateTests`); build 0 warn/0 err |
| 2026-07-01 | Task 14 | **Live Dashboard (server render) + JSON snapshot + TeamLeader object-level scoping + thin leader landing.** New `Areas/Admin/Controllers/ActivationsController.cs` (`[Route("admin/activations")]`, class gate `AdminCookieScheme`+`Roles="SystemAdmin,PlanManager,TeamLeader"`): `EnsureMayViewAsync(activationId,ct)` replicates the proven Phase-1 API "own teams" check (`Api/Controllers/ActivationsController.Dashboard`, DEC-17) verbatim — no-op for Manager/Admin, else loads the activation's `ActivationParticipant`s → distinct `TeamId`s → `Team`s and throws `AppException.Forbidden` unless the caller leads at least one; **both** `Dashboard` (`GET {id}`) and `Snapshot` (`GET {id}/snapshot`, returns `Json(dto)`) call this guard first, so a leader cannot bypass the HTML gate by hitting the JSON endpoint directly. `Dashboard` redirects to `.../summary` (Task-not-yet-built) once `dto.Status==Closed`, else renders `new DashboardVm(dto, CanAct: IsInRole(SystemAdmin/PlanManager))`. Leader `Index` (`GET ""`) redirects Manager/Admin to `/admin/plans`; a TeamLeader gets every Active `PlanActivation` with a participating team they lead (same participant→team→leader computation as the guard), each row linking to its dashboard. New `Areas/Admin/Models/DashboardVm.cs`: `DashboardVm(DashboardDto Dto, bool CanAct)` + `LeaderActivationsVm{Activations:[Row(Id,Shift,RosterDate)]}`. `Dashboard.cshtml`: 5 `counter-tile`s (`data-counter`/`data-value` attributes for precise testability; `ready`/`escalated` reuse the existing CSS colour classes), both rates hand-formatted as `{(int)Math.Round(rate*100)}%` (NOT the culture-sensitive `"P0"` format specifier — empirically verified `(0.5).ToString("P0", new CultureInfo("ar"))` renders `"50٪؜"`, the Arabic percent sign plus a hidden RTL mark, which would silently break both the visual "%" and any test asserting it), the team-ranking table rendered in the DTO's own Score-descending order (never re-sorted in the view) with the first/last row tagged `rank-best`/`rank-delayed` (new CSS-only classes, no new text — reuses the existing green/red tokens), the overdue list (red `list-group-item`), the latest-events feed, `data-activation-id` on the root element, and a disabled/placeholder action bar (Escalate/Broadcast/Close buttons, gated on `CanAct && Status==Active`, no `<form>`/antiforgery — Task 15 wires the real POSTs) plus the `scripts` section referencing `/lib/signalr/signalr.min.js` + `/js/dashboard.js` (Task 16 builds the latter). `Index.cshtml` is the plain leader-landing table (reuses `Review.Shift`/`Review.Date`/`Plans.WatchDashboard`/`Common.Actions`). New resx keys (ar+en, all 3 previously missing): `Dash.Title`, `Activations.Title`, `Activations.Empty` — the 14 `Dash.*`/`Summary.Title`/`Error.*` keys the brief listed as pre-existing were confirmed present and untouched. **Cross-cutting fix surfaced by this task's own tests:** `Program.cs` now calls `AddWebEncoders(o => o.TextEncoderSettings = new TextEncoderSettings(UnicodeRanges.All))` — discovered that .NET's default `HtmlEncoder` renders every Arabic character in every existing Arabic-first view as a numeric HTML entity (`&#x627;...`); harmless to the browser but made it impossible for any test (this one is the first to try) to assert real rendered Arabic text, and needlessly bloats every response. New `tests/.../Web/DashboardTests.cs` (5 tests, seeds its own Organization + PlanManager/2×TeamLeader/2×TeamMember idempotently; each test builds a Ready `Plan`+`Team`(s)+`TaskTemplate`+`ShiftAssignment` roster aligned to `TestAppFactory.FixedShift` then activates it via the real `IActivationService`, matching `ActivateTests`' arrangement): counters+rates render correctly (member1 acknowledges + one task manually marked Done → Total=2/Pending=1/Ready=1/Escalated=0/Inducted=0, both rates 50%) asserting the real Arabic `Dash.*` labels plus exact `data-counter`/`data-value` pairs and `data-rate` percentages; team ranking renders Score-descending using DELIBERATELY reverse-alphabetical team names ("Team Zulu" acknowledged/high-score must render before "Team Alpha" unacknowledged/zero-score) so the assertion can't pass by alphabetical accident; snapshot returns `application/json` containing camelCase `totalParticipants`; a TeamLeader who genuinely leads a team on a separate, never-activated plan (not this activation) is redirected to `/admin/denied` on BOTH the HTML dashboard and the JSON snapshot; an unauthenticated client hitting the dashboard is redirected to `/admin/login`. RED confirmed first (all 5 failed 404 — no route yet, controller/VM/views added after). | `(this commit)` | 154 (18 unit unchanged; 136 integration: 131 prior + 5 new `DashboardTests`); build 0 warn/0 err; 2 consecutive full-suite runs both fully green (no flake observed this run) |
| 2026-07-01 | Task 16 | **Realtime dashboard client (`dashboard.js`) + cookie-authenticated SignalR test + additive hub auth-scheme fix.** New `src/ExecPlan.Api/wwwroot/js/dashboard.js` (dependency-free, assumes the vendored global `signalR` + `fetch`): no-ops unless `[data-activation-id]` is present; `refresh()` fetches `GET /admin/activations/{id}/snapshot` (`credentials:'same-origin'`) and re-renders the 5 counters + 2 rates by id, plus ranking/overdue/feed by `innerHTML`; `dto.status===1` (Closed) redirects to `.../summary`; connects via `new signalR.HubConnectionBuilder().withUrl('/hubs/dashboard').withAutomaticReconnect().build()`, `JoinActivation(id)` on start, `onreconnecting/onclose→startPoll` + `onreconnected→stopPoll+refresh` (5s poll fallback). **Corrected the task brief's sample event name:** the server (`SignalRRealtimeNotifier`) actually pushes `"DashboardUpdated"` (verified in `Api/Hubs/SignalRRealtimeNotifier.cs`), not `"DashboardChanged"` as the brief's pseudocode showed — `dashboard.js` listens for `'DashboardUpdated'`. `Dashboard.cshtml`: added the additive id hooks `dashboard.js` targets (`n-total/n-pending/n-ready/n-escalated/n-inducted/n-response/n-taskrate` on the existing counter/rate elements — ordered so the pre-existing `data-counter=…`/`data-rate=…` substring assertions in `DashboardTests` still match verbatim — and wrapping `<div id="rank-body">`/`id="overdue-body"`/`id="feed-body"` around the existing Teams/Overdue/Events conditional blocks, unchanged otherwise) — no SSR content or structure changed beyond the wrapping divs and id attributes. **Approved additive hub-auth change (Decision A):** `DashboardHub` was bare `[Authorize]`, which — since `Program.cs` sets JWT bearer as the SOLE default scheme — only ever authenticated a JWT; a cookie-only browser session (the real MVC dashboard) could never join. Changed to `[Authorize(AuthenticationSchemes = $"{JwtBearerDefaults.AuthenticationScheme},{AuthPolicies.AdminCookieScheme}")]` (added `using Microsoft.AspNetCore.Authentication.JwtBearer;` + `using ExecPlan.Api.Auth;`) — purely additive: the mobile JWT path (`access_token` query string) is completely unaffected (confirmed: all 4 `RealtimeTests` still pass unchanged), and the AdminCookie now also authenticates the hub. New `tests/.../Web/DashboardRealtimeTests.cs` — a REAL cookie-authenticated `HubConnection` (not a fallback): arranges an Active activation the proven `DashboardTests` way (plan+team+membership+on-duty `ShiftAssignment` aligned to `TestAppFactory.FixedShift` + real `IActivationService.ActivateAsync`), logs in via the real cookie sign-in POST and extracts the raw `Set-Cookie` value (the connection's `HttpMessageHandlerFactory` talks directly to `TestServer`, bypassing the login client's own cookie jar), attaches it via `HttpConnectionOptions.Headers["Cookie"]` (long-polling transport, mirroring `RealtimeTests`' `AccessTokenProvider` pattern for JWT), joins the activation, triggers the push via the real `AcknowledgeService.AcknowledgeAsync` in a DI scope (the same production trigger `RealtimeTests` exercises over HTTP), and asserts a `"DashboardUpdated"` `DashboardDto` (`ReadyCount==1`) arrives within 10s. **The cookie-over-raw-TestServer-handler approach worked on the first attempt — the documented HTTP-snapshot fallback was NOT needed.** | `(this commit)` | 160 (18 unit unchanged; 142 integration: 141 prior + 1 new `DashboardRealtimeTests`); build 0 warn/0 err; 2 consecutive full-suite runs both fully green (no flake observed either run); the existing 4 `RealtimeTests` (JWT) re-verified green after the hub auth-scheme change |
| 2026-07-01 | Task 15 | **Dashboard action bar (run-escalation/broadcast/close) + Activation Summary.** Extended the existing `Areas/Admin/Controllers/ActivationsController` (Task 14) with 3 new POST actions + 1 new GET, injecting `IEscalationService`/`BroadcastService`/`ExecutionService` (concrete types, per the Phase-1 DI gotcha) alongside the existing `IDashboardService`/`IUnitOfWork`/`ICurrentUser`. Every new action is `[Authorize(AdminCookieScheme, Policy=ManagerOrAdmin)]` **on top of** the class-level `Roles="SystemAdmin,PlanManager,TeamLeader"` gate — narrows TeamLeader out of all 4, exactly per this task's global constraint (Manager/Admin only, no "own teams" scoping needed since these are global authority actions). `RunEscalation` (`POST {id}/run-escalation`) → `_esc.RunCycleAsync`, stashes `TempData["toast"]=$"+{AttemptsAdded}/{Inducted}"` (verbatim per the task brief), redirects to the dashboard. `Broadcast` (`POST {id}/broadcast`, `string body`) → `_broadcast.BroadcastAsync`, redirects to the dashboard. `Close` (`POST {id}/close`) → `_exec.CloseAsync`, redirects to `.../summary`. `Summary` (`GET {id}/summary`) re-uses `_dash.GetSnapshotAsync` — bounces back to the live dashboard if still `Active`, else renders `new DashboardVm(dto, false)` via the new `Summary.cshtml` (5 `counter-tile`s + rates + final Score-ranked team table, deliberately **no** action bar and **no** `@section scripts` per §16 "no live updates" — a closed activation is static). `Dashboard.cshtml`'s Task-14 disabled placeholder action bar is now real: two plain antiforgery-protected `<form>`s (Escalate, Close — Close wrapped in a JS `confirm()` using `Dash.CloseConfirm`) plus a Bootstrap 5 modal (`data-bs-toggle="modal"`, no extra script needed — `bootstrap.bundle.min.js` is already loaded by `_Layout`) containing the Broadcast form's `body` textarea; the action-bar wrapping `<div>` carries a `data-action-bar="true"` marker for unambiguous test/DOM targeting; `TempData["toast"]`, when present, renders as an `alert-info` prefixed by the new localized `Dash.ToastPrefix` label. New resx keys (ar+en): `Dash.BroadcastBody`, `Dash.Send`, `Dash.ToastPrefix`, `Summary.ClosedNote` — everything else reused Task 14's existing `Dash.*`/`Summary.Title`/`Common.Cancel` catalogue. **Leader-close 302-not-403 note:** per this app's established `AdminCookie` semantics (`AccessDeniedPath=/admin/denied`), a role-policy rejection for an already-authenticated principal is a 302 to `/admin/denied` (with a `ReturnUrl` query string), never a bare 403 — the task brief's own pseudocode comment said "→ 403", overridden per this task's explicit ambiguity resolution. New `tests/.../Web/DashboardActionsTests.cs` (5 tests, seeds its own Organization + PlanManager/TeamLeader/TeamMember idempotently; arranges one Ready `Plan`+`Team`+`TaskTemplate`+`ShiftAssignment` aligned to `TestAppFactory.FixedShift` then activates via the real `IActivationService`, same pattern as `DashboardTests`/`ActivateTests`): run-escalation → 302 back to dashboard; broadcast persists a `BroadcastMessage` row (verified in a fresh scope, matched on `ActivationId`+`Body`); close → 302 to `.../summary`, the summary body lacks `/run-escalation`/`/broadcast`/the close form action/`data-action-bar` but still renders the final `data-counter="total" data-value="1"`, and a fresh-scope `PlanActivation.Status==Closed`; summary of a still-Active activation → 302 back to the dashboard; a TeamLeader (who genuinely leads the activation's only team, so their dashboard GET legitimately succeeds and yields a valid antiforgery pair) POSTing close → 302 containing `/admin/denied` and, in a fresh scope, `PlanActivation.Status` still `Active` (the close never happened). RED confirmed first (all 5 failed 404 — no routes yet). | `(this commit)` | 159 (18 unit unchanged; 141 integration: 136 prior + 5 new `DashboardActionsTests`); build 0 warn/0 err; 2 consecutive full-suite runs both fully green (no flake observed either run) |
| 2026-07-01 | Task 17 (final) | **§21 web acceptance end-to-end test + Phase 2 living-docs close-out.** New `tests/.../Web/WebAcceptanceTests.cs` — the PRD §21 "Web" acceptance row, driven entirely in-process through the MVC admin area (never `/api/v1`), under the default `ar` culture: seeds a PlanManager + Organization + one TeamMember idempotently (same pattern as every other `Web` test class), signs in via `WebTestHelpers.NewClient`/`LoginAsync`, drives all 4 create-plan wizard steps (`PostFormAsync`/the repeated-key overload for `MemberUserIds`) to a `Ready` plan, activates it, then reads the live dashboard. **Confirmed the brief's KEY TRAP is real** (deliberately broke it first, red, then fixed): `PlanWizardController.BuildReviewVmAsync` pre-fills each roster row's GET default with `Shift=Morning`/`Date=DateTime.UtcNow.Date` (the real wall clock) but `ActivationService.ActivateAsync` resolves on-duty rows against the host's FIXED `IClock` (`TestAppFactory.FixedShift`) — posting the GET defaults verbatim redirects Activate back to Plan Detail with a Conflict ("no one on duty") instead of to the dashboard; the Finish POST instead sets `roster[0].Shift`/`roster[0].Date` explicitly to `TestAppFactory.FixedShift.Band`/`RosterDate` (`yyyy-MM-dd`), exactly like `WizardStep4Tests`/`ActivateTests` already do. Plan Detail assertion reads the real Arabic `Plans.Activate` string (and `dir="rtl"`/`btn-launch`) straight out of the running `IStringLocalizerFactory` forced to `ar` rather than a hand-typed literal, so it tracks `.ar.resx` verbatim; the post-activate dashboard assertion does the same for `Dash.Total/Pending/Ready/Escalated/Inducted` plus the `data-counter`/`data-value` numbers (Total=1/Pending=1, rest 0 — the single untouched roster row). A `requestedUrls` list records every URL the test hits and asserts none starts with `/api` — the "all in-process" proof. `docs/DECISIONS.md` gained **DEC-20..27** (DEC-20..26 copied from the design spec §13 verbatim: admin-area/cookie-scheme topology, in-process Application calls, the custom self-hosted Material theme resolving the DEC-12 follow-up, the server-incremental wizard, `AppExceptionMiddleware` content negotiation, the Organizations screen for FR-ADM-1, and the dashboard snapshot-refetch+poll-fallback client; **DEC-27 new** — `DashboardHub`'s dual JWT+AdminCookie auth scheme from Task 16, purely additive) and the now-resolved DEC-12 follow-up in "Open" was struck through with a pointer to DEC-22. **All 8 PRD §16 admin screens now ship:** login, users, departments, organizations, plans list/detail, create-plan wizard (4 steps), live dashboard + activation summary. Head commit for this task = the commit containing this entry (see `git log` — `test(web): §21 acceptance e2e; docs: DEC-20..27, progress, claude.md`, branch `feat/web-admin`). **Carried-forward note from Task 16:** the cookie-authenticated SignalR `DashboardRealtimeTests` push test passed on the first attempt over the raw `TestServer` handler with a `Cookie` header — the documented HTTP-snapshot-polling fallback described in that task's plan was **not** needed in tests (the real `dashboard.js` client still implements the 5s poll fallback for production hub-down scenarios, per DEC-26/NFR-1). | `(this commit)` | **161** (18 unit unchanged; 143 integration: 142 prior + 1 new `WebAcceptanceTests`); `dotnet build ExecPlan.sln` 0 warnings/0 errors; `dotnet test --filter FullyQualifiedName~WebAcceptanceTests` green; full suite `dotnet test ExecPlan.sln` green (18+143=161) |

## Phase 3 — Flutter mobile (separate repo) — not started
## Phase 4 — Polish — not started
